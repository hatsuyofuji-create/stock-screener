# このファイルを同じフォルダで「secrets.toml」という名前にコピーして、
# api_key に自分の Claude(Anthropic) APIキーを入れてください。
# （「AIで抽出する」をONにするときだけ必要。OFFなら不要です）
#
# ※ secrets.toml は秘密情報なので、他人に渡したりGitHubに上げたりしないこと。

[anthropic]
api_key = "sk-ant-ここに自分のAPIキー"
